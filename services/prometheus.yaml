apiVersion: v1
kind: Service
metadata:
  labels:
    name: nc-prometheus
  name: nc-prometheus
  namespace: nex-system
spec:
  ports:
  - port: 9090
    name: prometheus
    protocol: TCP
    targetPort: 80
  selector:
    name: webstork
  type: ${EXPOSETYPE}