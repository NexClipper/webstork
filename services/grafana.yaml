apiVersion: v1
kind: Service
metadata:
  labels:
    name: webstork-grafana
  name: webstork-grafana
  namespace: nex-system
spec:
  ports:
  - port: 80
    name: grafana
    protocol: TCP
    targetPort: 80
  selector:
    name: webstork
  type: ${EXPOSETYPE}
