apiVersion: v1
kind: Service
metadata:
  labels:
    name: nc-pushgateway
  name: nc-pushgateway
  namespace: nex-system
spec:
  ports:
  - port: 9091
    name: pushgateway
    protocol: TCP
    targetPort: 9091
  selector:
    name: webstork
  type: ${EXPOSETYPE}